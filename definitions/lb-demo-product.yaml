product: '1.0.0'

info:
  name: lb-demo
  title: lb-demo
  version: '1.0.0'

apis:
  'lb-demo':
    $ref: lb-demo.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
